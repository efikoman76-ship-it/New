[package]
name = "rhizome-kernels-metal"
description = "Metal backend: MSL kernels (feature `metal`)."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[features]
metal = []

[build-dependencies]

[dependencies]
rhizome-core = { workspace = true }

[lints]
workspace = true
