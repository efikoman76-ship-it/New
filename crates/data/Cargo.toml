[package]
name = "rhizome-data"
description = "Shard format, deterministic resumable sampling, packing, MinHash LSH dedup, synthetic generators."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
rhizome-core = { workspace = true }

[lints]
workspace = true
