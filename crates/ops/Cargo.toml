[package]
name = "rhizome-ops"
description = "Closed op vocabulary: shape inference, f64 reference implementations, adjoint rules, gradient checks."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
rhizome-core = { workspace = true }

[lints]
workspace = true
