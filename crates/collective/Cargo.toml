[package]
name = "rhizome-collective"
description = "Collective communication trait with the pure-Rust TCP backend used in CI; NCCL/RDMA behind features."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
rhizome-core = { workspace = true }

[lints]
workspace = true
